name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  common-ci:
    # reusable workflow from .github -repository
    # database-type: postgres / postgis
    uses: tuomas777/.github/.github/workflows/ci-django-api.yml@reusable_workflow_v2
    secrets: inherit
    with:
      python-version: 3.11
      database-type: postgres
